# caches / OS cruft
__pycache__/
*.pyc
.DS_Store
.venv/
logs/

# generated datasets and MCMC traces (large, regenerable / seeded)
data/
*.nc
*.zarr

# bulk per-run outputs: keep only the aggregated tables and the scaling table
results/*
!results/agg/
!results/agg40/
!results/scaling_results.csv

# scratch / throwaway
scripts/throw_away/